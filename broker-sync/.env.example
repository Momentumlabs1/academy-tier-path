# ── Broker MSSQL (credentials come from the broker after IP whitelisting) ──
MSSQL_HOST=
MSSQL_PORT=1433
MSSQL_DB=
MSSQL_USER=
MSSQL_PASSWORD=
# Only set to 1 if the broker endpoint uses a self-signed TLS certificate
MSSQL_TRUST_CERT=0

# ── Supabase (academy project: momentum-hq / qrgvltpakkubtkeukypa) ─────────
SUPABASE_URL=https://qrgvltpakkubtkeukypa.supabase.co
# Dashboard → Project Settings → API → service_role (NEVER commit this)
SUPABASE_SERVICE_ROLE_KEY=
