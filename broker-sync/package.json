{
  "name": "broker-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mirrors the TradeQuo/QM external IB SQL Server database into Supabase and rolls it up into the academy (deposits → tiers, trades → activity).",
  "scripts": {
    "sync": "node sync.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5",
    "mssql": "^11.0.1",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
